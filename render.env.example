# Example environment variables for Render (do NOT commit secrets)
# Copy values into Render Dashboard -> Service -> Environment

DJANGO_SETTINGS_MODULE=home.settings
# SECRET_KEY: provide a secure random string in the Render dashboard
SECRET_KEY=REPLACE_WITH_SECURE_KEY

# Primary database (Postgres) - Render provides this when you create a managed DB
# Example: postgres://user:password@hostname:5432/dbname
DATABASE_URL=postgres://user:password@hostname:5432/dbname

# Optional: separate chat DB URL if you want chat stored on Postgres instead of sqlite
# CHAT_DATABASE_URL=postgres://chat_user:chat_pass@hostname:5432/chatdb

# Hosts - comma-separated (no spaces required)
DEBUG=false
ALLOWED_HOSTS=your-app.onrender.com

# Optional email settings for transactional emails / password reset
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=your-smtp-user
# EMAIL_HOST_PASSWORD=your-smtp-password
# DEFAULT_FROM_EMAIL=no-reply@yourdomain.com

# Optional: if using S3 for media
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_STORAGE_BUCKET_NAME=
# AWS_S3_REGION_NAME=

# Render may provide the external URL in RENDER_EXTERNAL_URL; no need to set it
# RENDER_EXTERNAL_URL=https://your-app.onrender.com

# Keep this file out of version control when filling secrets; use Render's environment
# UI to manage actual secret values.
